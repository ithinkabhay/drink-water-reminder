/// Profile header and info rows (legacy exports kept for compatibility).
library;

export 'profile_widgets.dart'
    show
        ProfileAvatar,
        ProfilePhotoService,
        AchievementCard,
        ProfileDetailTile,
        ProfileValidators;
